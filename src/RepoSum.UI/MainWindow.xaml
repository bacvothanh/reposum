<Window x:Class="RepoSum.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RepoSum.UI"
        mc:Ignorable="d"
        Title="RepoSum" Height="720" Width="1100">
    <DockPanel Margin="12">
        <StackPanel DockPanel.Dock="Top" Orientation="Vertical" Margin="0,0,0,10">
            <Expander Header="Connection &amp; Repositories" IsExpanded="True">
                <Grid Margin="0,8,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*" />
                        <ColumnDefinition Width="3*" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Vertical" Margin="0,0,12,0">
                        <TextBlock Text="Organization URL" />
                        <TextBox Text="{Binding OrganizationUriText, UpdateSourceTrigger=PropertyChanged}" />

                        <TextBlock Margin="0,8,0,0" Text="Project" />
                        <TextBox Text="{Binding ProjectName, UpdateSourceTrigger=PropertyChanged}" />

                        <TextBlock Margin="0,8,0,0" Text="PAT (Personal Access Token)" />
                        <PasswordBox x:Name="PatPasswordBox" PasswordChanged="PatPasswordBox_OnPasswordChanged" />

                        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                            <Button Content="Load Repos" Command="{Binding LoadRepositoriesCommand}" Margin="0,0,8,0" />
                            <Button Content="Remove Selected" Command="{Binding RemoveSelectedRepositoriesCommand}" Margin="0,0,8,0" />
                            <Button Content="Save" Command="{Binding SaveSettingsCommand}" />
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Vertical">
                        <TextBlock Text="Select repositories" />
                        <ScrollViewer VerticalScrollBarVisibility="Auto" Height="170" Margin="0,4,0,0">
                            <ItemsControl ItemsSource="{Binding Repositories}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <CheckBox IsChecked="{Binding IsSelected}" Content="{Binding Name}" />
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </StackPanel>
                </Grid>
            </Expander>

            <DockPanel Margin="0,10,0,0">
                <StackPanel DockPanel.Dock="Left" Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Text="Date" Margin="0,0,6,0" />
                    <ComboBox Width="140"
                              ItemsSource="{Binding DateRangePresets}"
                              SelectedItem="{Binding SelectedDateRangePreset}" />

                    <TextBlock VerticalAlignment="Center" Text="Author" Margin="16,0,6,0" />
                    <TextBox Width="180" Text="{Binding AuthorFilter, UpdateSourceTrigger=PropertyChanged}" />

                    <TextBlock VerticalAlignment="Center" Text="Search" Margin="16,0,6,0" />
                    <TextBox Width="240" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />

                    <Button Content="Refresh" Command="{Binding RefreshCommand}" Margin="16,0,0,0" />
                </StackPanel>

                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock VerticalAlignment="Center" Text="{Binding StatusText}" />
                    <TextBlock VerticalAlignment="Center" Margin="12,0,0,0" Text="" />
                </StackPanel>
            </DockPanel>
        </StackPanel>

        <TabControl ItemsSource="{Binding Tabs}">
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Title}" />
                </DataTemplate>
            </TabControl.ItemTemplate>
            <TabControl.ContentTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.Resources>
                            <CollectionViewSource x:Key="GroupedItems" Source="{Binding Items}">
                                <CollectionViewSource.GroupDescriptions>
                                    <PropertyGroupDescription PropertyName="Category" />
                                </CollectionViewSource.GroupDescriptions>
                            </CollectionViewSource>
                        </Grid.Resources>

                        <ListView ItemsSource="{Binding Source={StaticResource GroupedItems}}">
                            <ListView.GroupStyle>
                                <GroupStyle>
                                    <GroupStyle.HeaderTemplate>
                                        <DataTemplate>
                                            <Border Background="{DynamicResource {x:Static SystemColors.ControlLightBrushKey}}" Padding="6" Margin="0,6,0,2">
                                                <TextBlock FontWeight="SemiBold" Text="{Binding Name}" />
                                            </Border>
                                        </DataTemplate>
                                    </GroupStyle.HeaderTemplate>
                                </GroupStyle>
                            </ListView.GroupStyle>

                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Border Padding="8" Margin="0,0,0,6" BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}" BorderThickness="1">
                                        <StackPanel>
                                            <DockPanel>
                                                <TextBlock DockPanel.Dock="Left" FontWeight="SemiBold" Text="{Binding Title}" />
                                                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                                                    <Button Content="🔗 Open"
                                                            ToolTip="Open in browser"
                                                            Command="{Binding DataContext.OpenWebUrlCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                            CommandParameter="{Binding}"
                                                            Margin="0,0,8,0" />
                                                    <Button Content="{Binding IsRead, Converter={x:Static local:BooleanToReadLabelConverter.Instance}}"
                                                            Command="{Binding DataContext.ToggleReadCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                            CommandParameter="{Binding}" />
                                                </StackPanel>
                                            </DockPanel>

                                            <TextBlock Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" FontSize="12" Margin="0,2,0,0">
                                                <Run Text="{Binding RepositoryName, Mode=OneWay}" />
                                                <Run Text="  •  " />
                                                <Run Text="{Binding Source, Mode=OneWay}" />
                                                <Run Text="  •  " />
                                                <Run Text="{Binding Author, Mode=OneWay}" />
                                                <Run Text="  •  " />
                                                <Run Text="{Binding Timestamp, Mode=OneWay}" />
                                            </TextBlock>

                                            <Expander Header="Details" Margin="0,6,0,0">
                                                <TextBlock TextWrapping="Wrap" Text="{Binding Details}" />
                                            </Expander>
                                        </StackPanel>
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsRead}" Value="True">
                                                        <Setter Property="Opacity" Value="0.6" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                    </Border>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Grid>
                </DataTemplate>
            </TabControl.ContentTemplate>
        </TabControl>
    </DockPanel>
</Window>
